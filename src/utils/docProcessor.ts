// Deprecated. The portfolio assistant no longer parses Word documents in the
// browser. Knowledge now lives as a curated, verified string inside the
// serverless function at /api/chat.ts, which keeps personal documents out of
// the public/ folder. This file is safe to delete (along with the `mammoth`
// dependency, if nothing else uses it).
export {};
